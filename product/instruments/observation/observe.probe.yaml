# Fixture specification for the `observe` verb.
# Grammar: docs/v2-readiness.md §4.

verb: observe
schemaVersion: 1

fixtures:
  - name: visible-button-on-known-screen
    description: |
      Observe a visible button on the policy-search screen.
      Exercises the role-first locator ladder's top rung.
      target.facet-id is rung-3 substrate metadata — the real
      observe verb ignores it; the rung-3 classifier reads it
      to render the correct facet on the synthetic substrate.
    input:
      surface:
        screen: policy-search
        facet-kind: element
      target:
        facet-id: policy-search:searchButton
        role: button
        name: "Search"
    expected:
      classification: matched
      error-family: null
    exercises:
      - rung: role
      - error-family: null

  - name: hidden-target-fails-not-visible
    description: |
      Observation should fail with not-visible when the target is
      hidden. Exercises the not-visible error family on the
      observe surface.
    input:
      surface:
        screen: policy-search
        facet-kind: element
      target:
        facet-id: policy-search:advancedOptionsButton
        role: button
        name: "Advanced Options"
      world-setup:
        hide-target: true
    expected:
      classification: failed
      error-family: not-visible
    exercises:
      - error-family: not-visible
