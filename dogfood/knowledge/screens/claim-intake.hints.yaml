screen: claim-intake
screenAliases:
  - claim intake
  - claim intake form
  - new claim
  - file a claim
elements:
  claimantName:
    aliases:
      - claimant name
      - claimant name field
      - name of claimant
    defaultValueRef: "{{claimant.fullName}}"
    parameter: claimantName
    affordance: text-entry
  incidentDate:
    aliases:
      - incident date
      - date of incident
      - when it happened
    defaultValueRef: "{{claim.incidentDate}}"
    parameter: incidentDate
    affordance: text-entry
  claimAmount:
    aliases:
      - claim amount
      - amount
      - dollar amount
      - claim value
    defaultValueRef: "{{claim.amount}}"
    parameter: claimAmount
    affordance: text-entry
  claimType:
    aliases:
      - claim type
      - type of claim
      - claim category
      - category dropdown
    defaultValueRef: "{{claim.type}}"
    parameter: claimType
    affordance: selection
  description:
    aliases:
      - description
      - claim description
      - details
      - incident details
    affordance: text-entry
  attachments:
    aliases:
      - upload attachments
      - attach files
      - add documents
    affordance: action
  submitClaim:
    aliases:
      - submit
      - submit claim
      - file claim
      - save claim
    affordance: action
  cancelClaim:
    aliases:
      - cancel
      - cancel claim
      - discard
      - go back
    affordance: action
  confirmationMessage:
    aliases:
      - confirmation
      - success message
      - claim submitted message
