screen: dashboard
screenAliases:
  - dashboard
  - main dashboard
  - home screen
  - overview
elements:
  summaryCards:
    aliases:
      - summary cards
      - overview cards
      - metric cards
      - kpi cards
  recentActivityTable:
    aliases:
      - recent activity
      - activity table
      - activity log
      - recent events
  alertsPanel:
    aliases:
      - alerts
      - alerts panel
      - notifications
      - warning alerts
  quickActionsMenu:
    aliases:
      - quick actions
      - action menu
      - shortcuts
  dateRangeFilter:
    aliases:
      - date range
      - date filter
      - date range filter
      - time period
    defaultValueRef: "{{dashboard.dateRange}}"
    parameter: dateRange
    affordance: text-entry
  statusFilter:
    aliases:
      - status filter
      - filter by status
      - status dropdown
    defaultValueRef: "{{dashboard.statusFilter}}"
    parameter: statusFilter
    affordance: selection
  exportButton:
    aliases:
      - export
      - export data
      - download report
      - export to csv
    affordance: action
  refreshButton:
    aliases:
      - refresh
      - refresh data
      - reload
      - update dashboard
    affordance: action
